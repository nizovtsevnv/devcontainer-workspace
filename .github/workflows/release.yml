name: Create Release

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Get previous tag
        id: previoustag
        run: |
          # Получить предыдущий тег
          PREV_TAG=$(git tag --sort=-version:refname | grep -E '^v[0-9]+\.[0-9]+\.[0-9]+$' | sed -n '2p')
          if [ -z "$PREV_TAG" ]; then
            # Если это первый тег, использовать первый коммит
            PREV_TAG=$(git rev-list --max-parents=0 HEAD)
          fi
          echo "tag=$PREV_TAG" >> $GITHUB_OUTPUT
          echo "Previous tag: $PREV_TAG"

      - name: Generate changelog
        id: changelog
        run: |
          CURRENT_TAG=${GITHUB_REF#refs/tags/}
          PREV_TAG="${{ steps.previoustag.outputs.tag }}"

          echo "Generating changelog from $PREV_TAG to $CURRENT_TAG"

          # Получить все коммиты между тегами
          COMMITS=$(git log --pretty=format:"%s|%b" "$PREV_TAG..$CURRENT_TAG")

          # Инициализация секций
          FEATURES=""
          FIXES=""
          CHORES=""
          BREAKING=""

          # Парсинг коммитов
          while IFS='|' read -r subject body; do
            # Проверка на BREAKING CHANGE
            if echo "$body" | grep -q "BREAKING CHANGE:"; then
              BREAKING_MSG=$(echo "$body" | grep "BREAKING CHANGE:" | sed 's/BREAKING CHANGE: //')
              BREAKING="${BREAKING}- ${subject}\n  ${BREAKING_MSG}\n"
            fi

            # Группировка по типам
            case "$subject" in
              feat:*|feat\(*)
                FEATURES="${FEATURES}- ${subject#feat: }\n"
                FEATURES="${FEATURES}- ${subject#feat\(}\n"
                ;;
              fix:*|fix\(*)
                FIXES="${FIXES}- ${subject#fix: }\n"
                FIXES="${FIXES}- ${subject#fix\(}\n"
                ;;
              chore:*|chore\(*|refactor:*|refactor\(*|docs:*|docs\(*|style:*|style\(*|test:*|test\(*)
                CHORES="${CHORES}- ${subject}\n"
                ;;
              *)
                CHORES="${CHORES}- ${subject}\n"
                ;;
            esac
          done <<< "$COMMITS"

          # Формирование changelog
          CHANGELOG="## 🎯 Основные изменения\n\n"

          if [ -n "$BREAKING" ]; then
            CHANGELOG="${CHANGELOG}### ⚠️ Breaking Changes\n\n${BREAKING}\n"
          fi

          if [ -n "$FEATURES" ]; then
            CHANGELOG="${CHANGELOG}### ✨ Новые возможности\n\n${FEATURES}\n"
          fi

          if [ -n "$FIXES" ]; then
            CHANGELOG="${CHANGELOG}### 🐛 Исправления\n\n${FIXES}\n"
          fi

          if [ -n "$CHORES" ]; then
            CHANGELOG="${CHANGELOG}### 🔧 Техническое\n\n${CHORES}\n"
          fi

          # Добавить ссылки на документацию
          CHANGELOG="${CHANGELOG}## 📚 Документация\n\n"
          CHANGELOG="${CHANGELOG}- [Система автоматизации Makefile](https://github.com/${{ github.repository }}/blob/main/doc/devenv/makefile.md)\n"
          CHANGELOG="${CHANGELOG}- [Руководство по DevContainer](https://github.com/${{ github.repository }}/blob/main/doc/devenv/devcontainer.md)\n"
          CHANGELOG="${CHANGELOG}- [Файловая структура](https://github.com/${{ github.repository }}/blob/main/doc/devenv/file-tree.md)\n"

          # Сохранить в файл
          echo -e "$CHANGELOG" > changelog.md
          cat changelog.md

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          body_path: changelog.md
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
